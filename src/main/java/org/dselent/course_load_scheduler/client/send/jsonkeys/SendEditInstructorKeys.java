package org.dselent.course_load_scheduler.client.send.jsonkeys;

public enum SendEditInstructorKeys {	
	ID,
	RANK,
	FIRST_NAME,
	LAST_NAME,
	EMAIL,
	COURSE_LOAD_ID,
	COURSE_LOAD_TYPE,
	COURSE_LOAD_DESCRIPTION,
	DELETED;
}
